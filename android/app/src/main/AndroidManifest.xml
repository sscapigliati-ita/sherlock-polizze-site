<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.sherlock.polizze">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Android 11+ package visibility: per poter risolvere l'intent
         MediaStore.ACTION_IMAGE_CAPTURE serve dichiarare che l'app
         "interroga" app che gestiscono quell'action. Senza questo blocco
         resolveActivity() puo' restituire null anche se una camera app
         e' installata. -->
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="false">
        <!-- Firebase Analytics + Google Analytics: raccolta DISABILITATA per default.
             Attivazione esplicita solo dopo consenso utente (vedi
             MainActivity.setAnalyticsConsent chiamato dal JS bridge dopo la
             scelta nel banner in WebView). Coerente con audit R4.
             Ref: https://firebase.google.com/docs/analytics/configure-data-collection -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_analytics_storage"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_ad_storage"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_ad_user_data"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_ad_personalization_signals"
            android:value="false" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Android App Links: click su www.sherlockpolizze.it aprono direttamente l'app.
                 autoVerify=true fa il check assetlinks.json in modo automatico.
                 NOTA: NON dichiariamo l'host apex "sherlockpolizze.it" perche' Vercel lo redirige
                 308->www a livello di dominio, e Google non segue redirect per la verifica di
                 /.well-known/assetlinks.json (falliva "domini web non associati"). Se in futuro
                 si vuole coprire anche apex, spostare il redirect apex->www in un middleware
                 Astro che escluda /.well-known/ e cambiare la config dominio in Vercel Dashboard. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="www.sherlockpolizze.it" />
            </intent-filter>
        </activity>

        <!-- FileProvider per esporre l'Uri della foto scattata dalla
             camera app tramite MediaStore.EXTRA_OUTPUT. L'authority
             deve corrispondere a FILE_PROVIDER_AUTHORITY in MainActivity. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="it.sherlock.polizze.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
